# test-helper
for my beloved
1. install git at https://git-scm.com/install/windows
2. use this command in cmd:git clone https://github.com/eternals4dness/test-helper.git
3. install docker desktop
4. use this command in cmd(go to ur directory):docker build -t test-helper
5. use this command to run the program whenever:docker run -ti testhelper
